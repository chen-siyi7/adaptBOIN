% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproduce.R
\name{reproduce_paper}
\alias{reproduce_paper}
\title{Regenerate every table and figure in the manuscript}
\usage{
reproduce_paper(
  out_dir = "adaptboin_output",
  n_sim = 2000L,
  n_cores = 1L,
  do_figures = TRUE,
  do_studies = TRUE,
  do_reference = TRUE,
  params = adapt_params()
)
}
\arguments{
\item{out_dir}{Output directory; created if absent.}

\item{n_sim}{Number of simulated trials per cell.}

\item{n_cores}{Number of cores. Fork-based, so values above one have no
effect on Windows.}

\item{do_figures}{Write figures.}

\item{do_studies}{Run the steepness, offset-MTD and elimination studies.}

\item{do_reference}{Run the independent MCMC posterior diagnostic.}

\item{params}{Parameter list.}
}
\value{
Invisibly, a list holding every result object.
}
\description{
Runs the primary grid, the sensitivity analyses, the targeted comparator
studies and the elimination-rule comparison, writing CSV files and, if
\pkg{ggplot2} is installed, PDF figures.
}
\details{
The full run at 2000 trials is slow, chiefly because the two Bernstein
configurations draw 2000 importance samples per trial. Start with a small
\code{n_sim} to confirm the pipeline works.
}
\examples{
\dontrun{
reproduce_paper(n_sim = 200L)                     # quick check
reproduce_paper(n_sim = 2000L, n_cores = 6L)      # full run
}
}
